# 2026-04-08 잔여 작업 — Apple Token Revoke 운영 검증 후

## 배경

- PR #41 (Apple Token Revoke) develop 머지 완료
- PR #42 (develop → main) merge -s ours로 conflict 해결 후 머지 완료
- 운영 배포 + Apple 환경변수 등록 (`APPLE_TEAM_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY`)
- 부팅 로그 `Apple OAuth enabled` 확인
- iOS 실기기에서 탈퇴 → Apple 측 연결 해제까지 확인 ✅

App Store 5.1.1(v) 요건 충족 완료. 아래는 후속 정리 작업.

---

## 오늘 처리 (간단)

### 1. Stale 브랜치 정리

머지 완료된 feat 브랜치 삭제. **`feat/backend-tasks`는 절대 삭제 금지** (재구현 대기 중).

| 브랜치 | 상태 | 처리 |
|--------|------|------|
| `feat/apple-token-revoke` | PR #41 머지 완료 | 로컬 + 원격 삭제 |
| `feat/fcm-production` | PR #34 머지 완료 (squash) | 로컬 + 원격 삭제 |
| `feat/dead-letter-schedulers` | PR #37 머지 완료 (squash) | 로컬 + 원격 삭제 |
| `feat/backend-tasks` | **재구현 대기** | ❌ 삭제 금지 |

명령:
```bash
# 로컬
git branch -d feat/apple-token-revoke
git branch -d feat/fcm-production
git branch -d feat/dead-letter-schedulers

# 원격 (이미 GitHub에서 자동 삭제됐을 수 있음)
git push origin --delete feat/apple-token-revoke
git push origin --delete feat/fcm-production
git push origin --delete feat/dead-letter-schedulers
```

---

## 별도 세션 (오늘 X)

### 2. `feat/backend-tasks` 5개 작업 재구현

`feat/backend-tasks` 브랜치는 merge-base가 `3fe67a4` (#37 머지 직후)라 develop과 17개 파일이 충돌함. 직접 머지 불가 → develop 베이스 새 브랜치에 재구현 필요.

**원본 commit**:
- `b644d74` — 회원탈퇴 자동 위임 + ACTIVE 크루 탈퇴 + FCM 트랜잭션 분리 + 크루 최소 기간 검증 + 빈 크루 자동 정리
- `741a032` — 챌린지 알림 임시 비활성화

**작업 항목** (5개) — Part 2 매핑 기준 정정:
- ✅ **BE-P0-1**: 회원탈퇴 시 LEADER 자동 위임 — `feat/leader-auto-delegate`로 별도 진행 (2026-04-08, 본 항목 완료)
- BE-P0-2: 크루 최소 기간 7일 검증 (`CR024`)
- BE-P1-1: ACTIVE 크루 챌린지 미시작 멤버 탈퇴 허용 (`CR025`)
- BE-P1-2: 챌린지 성공 알림 `@TransactionalEventListener`로 트랜잭션 밖 분리 (FCM 트랜잭션 분리)
- BE-P1-3: 빈 크루 자동 정리 — 방어 로직 (`LeaveCrewService`에서 `currentMembers == 0`이면 `deleteById`)
- 알림 임시 비활성화 (`741a032`)

**정책 결정 (2026-04-08 확정)**:
- LEADER 탈퇴 → **자동 위임** 채택. 가장 오래된 멤버(`joined_at ASC`, 탈퇴자 제외)에게 LEADER 역할 + `crews.creator_id` 이관 후 본인 제거
- 위임자 수동 선택 UI는 Phase 2 future-considerations로 보류

**연쇄 영향 (BE-P0-1 머지 시 처리됨)**:
- `docs/log/future-considerations.md` W1 (LEADER race condition) 항목 → 삭제 완료. 블로그 글감은 `docs/blog/draft/blog-withdraw-race-tradeoff-draft.md`로 분리 보존

### 3. future-considerations.md W1 추적 ✅ 처리 완료 (2026-04-08)

- BE-P0-1 머지로 race condition 자체가 사라져 W1 항목 삭제
- 블로그 글감 메모는 `docs/blog/draft/blog-withdraw-race-tradeoff-draft.md`로 분리 보존